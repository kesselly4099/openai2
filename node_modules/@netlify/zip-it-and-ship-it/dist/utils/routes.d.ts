export type Route = {
    pattern: string;
    methods: string[];
} & ({
    literal: string;
} | {
    expression: string;
});
export declare const getRoutesFromPath: (path: unknown, functionName: string, methods: string[]) => Route[];
